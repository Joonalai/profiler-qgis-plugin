[pytest]
timeout = 100

env_override_existing_values = 1

log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)-10s (%(filename)s) %(message)s
log_cli_date_format = %H:%M:%S

; Set to true when you want to see the GUI elements
qgis_qui_enabled = false
